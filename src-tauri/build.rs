fn main() {
    tauri_build::build()
}

// Additional macOS-specific build configurations can be added here
// For example, setting LSUIElement to hide from dock initially
// This would be done in Info.plist which Tauri generates
